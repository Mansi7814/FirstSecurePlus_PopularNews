<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:id="@+id/errorLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:background="@color/colorBackground"
    android:visibility="visible">

    <ImageView
        android:id="@+id/errorImage"
        android:src="@drawable/no_result"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:contentDescription="@string/error_image" />
    <TextView
        android:id="@+id/errorTitle"
        android:text="@string/error_title"
        android:textColor="@color/colorTextTitle"
        android:textStyle="bold"
        android:fontFamily="sans-serif-light"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/errorImage"
        android:gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" tools:targetApi="jelly_bean" />

    <TextView
        android:id="@+id/errorMessgae"
        android:text="@string/error_message"
        android:textColor="@color/colorTextSubtitle"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/errorImage"
        android:gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btnRetry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/retry"
        android:backgroundTint="@color/colorAccent"
        android:textColor="#fff"
        android:layout_below="@+id/errorMessgae"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" tools:targetApi="lollipop" />

</RelativeLayout>